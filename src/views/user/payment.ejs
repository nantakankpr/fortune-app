<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title>
        โอนเงิน - Fortune App
    </title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: white;
            min-height: 100vh;
            color: #333;
        }

        .container {
            width: 100%;
            max-width: none;
            margin: 0;
            background: white;
            min-height: 100vh;
            position: relative;
        }

        .header {
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
            padding: 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '💰';
            position: absolute;
            top: 10px;
            left: 20px;
            font-size: 24px;
            animation: sparkle 2s infinite;
        }

        .header::after {
            content: '💳';
            position: absolute;
            top: 15px;
            right: 25px;
            font-size: 20px;
            animation: sparkle 2s infinite 1s;
        }

        @keyframes sparkle {

            0%,
            100% {
                transform: scale(1) rotate(0deg);
                opacity: 0.7;
            }

            50% {
                transform: scale(1.2) rotate(180deg);
                opacity: 1;
            }
        }

        .welcome-text {
            font-size: 28px;
            font-weight: bold;
            color: white;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
            margin-bottom: 8px;
        }

        .subtitle {
            font-size: 16px;
            color: rgba(255, 255, 255, 0.9);
        }

        .content {
            padding: 20px 15px;
        }

        .payment-card {
            background: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%);
            padding: 25px;
            border-radius: 20px;
            color: white;
            margin-bottom: 20px;
            box-shadow: 0 8px 25px rgba(132, 250, 176, 0.3);
            text-align: center;
        }

        .payment-title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .transaction-id {
            font-size: 14px;
            opacity: 0.8;
            margin-bottom: 15px;
            font-family: monospace;
        }

        .amount-display {
            font-size: 48px;
            font-weight: bold;
            margin: 15px 0;
            text-shadow: 0 3px 6px rgba(0, 0, 0, 0.3);
        }

        .amount-text {
            font-size: 18px;
            opacity: 0.9;
            margin-bottom: 10px;
        }

        .qr-section {
            background: white;
            border-radius: 20px;
            padding: 25px;
            text-align: center;
            margin-bottom: 20px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
        }

        .qr-title {
            font-size: 20px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .qr-container {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            display: inline-block;
            box-shadow: inset 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .qr-image {
            max-width: 250px;
            width: 100%;
            height: auto;
            border-radius: 10px;
            border: 3px solid #fff;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .download-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 12px 25px;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            cursor: pointer;
            margin: 10px 5px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
        }

        .download-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        }

        .recipient-section {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .recipient-title {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .recipient-info {
            background: white;
            border-radius: 12px;
            padding: 15px;
            margin-bottom: 10px;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }

        .info-row:last-child {
            border-bottom: none;
        }

        .info-label {
            font-weight: 600;
            color: #666;
            font-size: 16px;
        }

        .info-value {
            color: #333;
            font-size: 16px;
            font-weight: 500;
        }

        .info-value.highlight {
            color: #667eea;
            font-weight: bold;
        }

        @keyframes pulse {

            0%,
            100% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.1);
            }
        }

        .status-text {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            margin-bottom: 8px;
        }

        .status-description {
            font-size: 14px;
            color: #666;
            line-height: 1.5;
        }

        .instructions {
            background: white;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            border-left: 4px solid #667eea;
        }

        .instructions-title {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .instruction-list {
            list-style: none;
            counter-reset: step-counter;
        }

        .instruction-list li {
            padding: 10px 0;
            padding-left: 30px;
            position: relative;
            color: #666;
            font-size: 16px;
            line-height: 1.5;
        }

        .instruction-list li::before {
            content: counter(step-counter);
            counter-increment: step-counter;
            position: absolute;
            left: 0;
            top: 10px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: bold;
        }

        .back-btn {
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            color: #333;
            padding: 12px 25px;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            margin: 10px 0;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(252, 182, 159, 0.3);
            text-decoration: none;
            display: inline-block;
            text-align: center;
            font-size: 16px;
        }

        .back-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(252, 182, 159, 0.4);
        }

        .status-btn {
            background: linear-gradient(135deg, #4ecdc4, #44a08d);
            color: white;
            padding: 12px 25px;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            margin: 10px 0;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(78, 205, 196, 0.3);
            text-decoration: none;
            display: inline-block;
            text-align: center;
            font-size: 16px;
        }

        .status-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(78, 205, 196, 0.4);
        }

        .loading-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(5px);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }

        .loading-content {
            text-align: center;
            padding: 30px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        }

        .loading-spinner {
            font-size: 40px;
            animation: spin 1s linear infinite;
            margin-bottom: 15px;
        }

        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }

            100% {
                transform: rotate(360deg);
            }
        }

        /* Success Modal Styles */
        .modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(8px);
            z-index: 2000;
            justify-content: center;
            align-items: center;
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .modal-content {
            background: white;
            border-radius: 25px;
            padding: 40px 30px;
            text-align: center;
            max-width: 400px;
            width: 90%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            transform: scale(0.9);
            animation: modalPop 0.3s ease forwards;
            position: relative;
            overflow: hidden;
        }

        @keyframes modalPop {
            to {
                transform: scale(1);
            }
        }

        .modal-content::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(135deg, #ff6b6b, #feca57, #48dbfb, #ff9ff3);
            background-size: 200% 100%;
            animation: rainbowMove 3s ease-in-out infinite;
        }

        @keyframes rainbowMove {

            0%,
            100% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }
        }

        .modal-icon {
            font-size: 80px;
            margin-bottom: 20px;
            animation: bounce 0.6s ease;
        }

        @keyframes bounce {

            0%,
            20%,
            53%,
            80%,
            100% {
                transform: translateY(0);
            }

            40%,
            43% {
                transform: translateY(-20px);
            }

            70% {
                transform: translateY(-10px);
            }

            90% {
                transform: translateY(-5px);
            }
        }

        .modal-title {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .modal-message {
            font-size: 16px;
            color: #666;
            line-height: 1.6;
            margin-bottom: 25px;
        }

        .modal-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 15px 40px;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            cursor: pointer;
            font-size: 16px;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
            position: relative;
            overflow: hidden;
        }

        .modal-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(102, 126, 234, 0.4);
        }

        .modal-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s;
        }

        .modal-btn:hover::before {
            left: 100%;
        }

        /* Slip Upload Styles */
        .upload-section {
            margin-bottom: 20px;
        }

        .slip-preview {
            background: white;
            border-radius: 15px;
            padding: 20px;
            margin-top: 15px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
        }

        .slip-preview-title {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            text-align: center;
        }

        .slip-preview-container {
            text-align: center;
        }

        .slip-image {
            max-width: 100%;
            max-height: 400px;
            border-radius: 10px;
            border: 2px solid #eee;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            margin-bottom: 15px;
        }

        .slip-actions {
            display: flex;
            gap: 10px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .upload-btn {
            background: linear-gradient(135deg, #48bb78, #38a169);
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 20px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(72, 187, 120, 0.3);
            font-size: 14px;
        }

        .upload-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(72, 187, 120, 0.4);
        }

        .clear-btn {
            background: linear-gradient(135deg, #a8a8a8, #888888);
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 20px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(168, 168, 168, 0.3);
            font-size: 14px;
        }

        .clear-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(168, 168, 168, 0.4);
            background: linear-gradient(135deg, #999999, #777777);
        }

        /* เพิ่ม style สำหรับปุ่มลบรูปแบบ secondary */
        .clear-btn-secondary {
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            color: #333;
            padding: 10px 20px;
            border: none;
            border-radius: 20px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(252, 182, 159, 0.3);
            font-size: 14px;
        }

        .clear-btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(252, 182, 159, 0.4);
        }

        /* หรือใช้สีน้ำเงินอ่อน */
        .clear-btn-blue {
            background: linear-gradient(135deg, #74b9ff, #0984e3);
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 20px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(116, 185, 255, 0.3);
            font-size: 14px;
        }

        .clear-btn-blue:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(116, 185, 255, 0.4);
        }

        /* Responsive Design */
        @media (min-width: 768px) and (max-width: 1024px) {
            .container {
                max-width: 100%;
                padding: 0;
            }

            .header {
                padding: 40px;
            }

            .welcome-text {
                font-size: 32px;
            }

            .content {
                padding: 30px 20px;
            }

            .amount-display {
                font-size: 56px;
            }

            .qr-image {
                max-width: 300px;
            }
        }

        @media (min-width: 1025px) {
            .container {
                max-width: 600px;
                margin: 0 auto;
                box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
            }

            .header {
                padding: 45px;
            }

            .welcome-text {
                font-size: 36px;
            }

            .content {
                padding: 35px 25px;
            }

            .amount-display {
                font-size: 64px;
            }

            .qr-image {
                max-width: 350px;
            }
        }

        @media (max-width: 767px) {
            .header {
                padding: 25px;
            }

            .welcome-text {
                font-size: 24px;
            }

            .content {
                padding: 15px 10px;
            }

            .payment-card {
                padding: 20px;
            }

            .amount-display {
                font-size: 36px;
            }

            .qr-image {
                max-width: 200px;
            }

            .info-row {
                flex-direction: column;
                gap: 5px;
                align-items: flex-start;
            }

            .download-btn,
            .back-btn,

            .slip-actions {
                flex-direction: column;
                gap: 8px;
            }

            .upload-btn,
            .clear-btn {
                width: 100%;
                font-size: 14px;
            }

            .slip-image {
                max-height: 300px;
            }

            .modal-content {
                padding: 30px 20px;
            }

            .modal-icon {
                font-size: 60px;
            }

            .modal-title {
                font-size: 20px;
            }
        }

        @media (max-width: 320px) {
            .payment-title {
                font-size: 20px;
            }

            .amount-display {
                font-size: 28px;
            }

            .qr-image {
                max-width: 180px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="welcome-text">โอนเงิน</div>
            <div class="subtitle">Fortune App - ชำระเงินแพ็คเกจ</div>
        </div>

        <div class="content">
            <!-- Instructions -->
            <div class="instructions">
                <div class="instructions-title">
                    📋 วิธีการโอนเงิน
                </div>
                <ol class="instruction-list">
                    <li>เปิดแอปธนาคารหรือ Mobile Banking ของคุณ</li>
                    <li>เลือกเมนู "โอนเงิน" หรือ "PromptPay"</li>
                    <li>สแกน QR Code ที่แสดงด้านล่าง หรือกรอกเบอร์โทร</li>
                    <li>ตรวจสอบจำนวนเงินให้ถูกต้อง (<%= paymentData.amount %> บาท)</li>
                    <li>ยืนยันการโอนเงิน</li>
                    <li>อัพโหลดรูปภาพสลีปและกดตรวจสอบ</li>
                </ol>
            </div>

            <!-- Payment Summary Card -->
            <div class="payment-card">
                <div class="payment-title">💰 รายการโอนเงิน</div>
                <!-- Recipient Information -->
                <div class="recipient-section">
                    <div class="recipient-title">
                        👤 ข้อมูลผู้รับเงิน (พร้อมเพย์)
                    </div>
                    <div class="recipient-info">
                        <div class="info-row">
                            <span class="info-label">เบอร์โทรศัพท์:</span>
                            <span class="info-value highlight">
                                <%= paymentData.recipient.mobileNumber %>
                            </span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">ชื่อ - นามสกุล:</span>
                            <span class="info-value">
                                <%= paymentData.recipient.fullName %>
                            </span>
                        </div>
                    </div>
                </div>
                <!-- QR Code Section -->
                <div class="qr-section">
                    <div class="qr-title">
                        📱 สแกน QR Code เพื่อโอนเงิน (พร้อมเพย์)
                    </div>
                    <!-- รูปธนาคาร -->
                    <div style="margin-bottom: 10px;">
                        <img src="/image/BKKB.webp" alt="Bank Logo" style="width: 50px; height: auto;">
                    </div>
                    <!-- remark แจ้งเตือนสำหรับธนาคารกรุงเทพ -->
                    <div style="font-size: 16px; color: #ff6b6b; margin-bottom: 10px;">
                        <strong>หมายเหตุ: สำหรับลูกค้าธนาคารกรุงเทพ</strong>
                        <p>
                            การตรวจสอบสลิปธนาคารกรุงเทพอาจใช้เวลา 5-10 นาที
                            หากต้องการความรวดเร็ว ขอแนะนำให้ใช้บริการธนาคารกสิกรไทยหรือธนาคารออมสิน
                        </p>
                    </div>
                    <div class="qr-container">
                        <img src="<%= paymentData.qrImageUrl %>" alt="QR Code" class="qr-image" id="qrImage">
                    </div>
                </div>
                <div class="transaction-id">
                    รหัสรายการ: <%= paymentData.transactionId %>
                </div>
                <div class="amount-display">
                    <%= paymentData.amount %> ฿
                </div>
                <div class="amount-text">
                    แพ็คเกจ: <%= paymentData.package.name %>
                </div>
                <div style="font-size: 14px; opacity: 0.8;">
                    สร้างเมื่อ: <%= paymentData.createdAt %>
                </div>
            </div>

            <!-- Upload Slip Section -->
            <div class="upload-section">
                <input type="file" id="slipUpload" accept="image/*" style="display: none;" onchange="previewSlip(this)">
                <button onclick="document.getElementById('slipUpload').click()" class="download-btn"
                    style="width: 100%; margin-bottom: 10px; background: linear-gradient(135deg, #667eea, #764ba2);">
                    📸 อัพโหลดสลิปการโอน
                </button>

                <!-- Image Preview -->
                <div id="slipPreview" class="slip-preview" style="display: none;">
                    <div class="slip-preview-title">
                        🖼️ รูปสลิปที่อัพโหลด
                    </div>
                    <div class="slip-preview-container">
                        <img id="slipImage" src="" alt="Slip Preview" class="slip-image">
                        <div class="slip-actions">
                            <button onclick="checkOrderStatus()" class="status-btn"
                                style="width: 100%; margin-bottom: 10px;">
                                🔍 ตรวจสอบสถานะออร์เดอร์
                            </button>
                            <button onclick="clearSlip()" class="clear-btn-blue">
                                🗑️ ลบรูป
                            </button>
                        </div>
                    </div>
                </div>

            </div>

        </div>
    </div>

    <!-- Loading Overlay -->
    <div class="loading-overlay" id="loadingOverlay">
        <div class="loading-content">
            <div class="loading-spinner">⏳</div>
            <div>กำลังตรวจสอบสถานะ...</div>
        </div>
    </div>

    <!-- Success Modal -->
    <div class="modal-overlay" id="successModal">
        <div class="modal-content">
            <div class="modal-icon">🎉</div>
            <div class="modal-title">ยกเลิกสำเร็จ!</div>
            <div class="modal-message">
                การยกเลิกออร์เดอร์ของคุณเสร็จสิ้นแล้ว<br>
                ขอบคุณที่ใช้บริการ Fortune App<br>
                หวังว่าจะได้ให้บริการคุณอีกครั้งในอนาคต ✨
            </div>
            <button onclick="closeApp()" class="modal-btn">
                ตกลง
            </button>
        </div>
    </div>

    <script>

        let selectedSlipFile = null;

        // Preview uploaded slip
        function previewSlip(input) {
            const file = input.files[0];
            if (file) {
                // Validate file type
                if (!file.type.startsWith('image/')) {
                    alert('กรุณาเลือกไฟล์รูปภาพเท่านั้น');
                    return;
                }

                // Validate file size (max 5MB)
                if (file.size > 5 * 1024 * 1024) {
                    alert('ไฟล์รูปภาพต้องมีขนาดไม่เกิน 5MB');
                    return;
                }

                selectedSlipFile = file;

                const reader = new FileReader();
                reader.onload = function (e) {
                    const slipImage = document.getElementById('slipImage');
                    const slipPreview = document.getElementById('slipPreview');

                    slipImage.src = e.target.result;
                    slipPreview.style.display = 'block';

                    // Scroll to preview
                    slipPreview.scrollIntoView({ behavior: 'smooth', block: 'center' });
                };
                reader.readAsDataURL(file);
            }
        }

        // Clear slip preview
        function clearSlip() {
            selectedSlipFile = null;
            document.getElementById('slipUpload').value = '';
            document.getElementById('slipPreview').style.display = 'none';
        }

        // Check order status
        function checkOrderStatus() {
            const loadingOverlay = document.getElementById('loadingOverlay');
            const loadingContent = loadingOverlay.querySelector('.loading-content');
            loadingContent.innerHTML = `
        <div class="loading-spinner">🔍</div>
        <div>กำลังตรวจสอบสถานะออร์เดอร์...</div>
    `;
            loadingOverlay.style.display = 'flex';

            // Create FormData to send both transaction ID and slip image
            const formData = new FormData();
            formData.append('transactionId', '<%= paymentData.transactionId %>');

            // Add slip image if available
            if (selectedSlipFile) {
                formData.append('slipImage', selectedSlipFile);
            }

            fetch('/order/status', {
                method: 'POST',
                credentials: 'include', // Include cookies for session management
                headers: {
                    'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]')?.getAttribute('content')
                },
                body: formData // Send FormData directly, not JSON.stringify
            })
                .then(response => response.json())
                .then(data => {
                    loadingOverlay.style.display = 'none';

                    if (data.success) {
                        let statusMessage = '';
                        let statusIcon = '';

                        switch (data.status) {
                            case 'pending':
                                statusIcon = '⏳';
                                statusMessage = 'รอการยืนยันการชำระเงิน';
                                break;
                            case 'completed':
                                statusIcon = '✅';
                                statusMessage = 'การชำระเงินได้รับการยืนยันแล้ว';
                                break;
                            case 'canceled':
                                statusIcon = '❌';
                                statusMessage = 'ออร์เดอร์ถูกยกเลิกแล้ว';
                                break;
                            default:
                                statusIcon = 'ℹ️';
                                statusMessage = data.message || 'ไม่พบข้อมูลสถานะ';
                        }

                        alert(`${statusIcon} สถานะออร์เดอร์: ${statusMessage}`);

                        // If completed, refresh page to show updated status
                        if (data.status === 'completed') {
                            window.location.href = '/order/succeeded';
                        }
                    } else {
                        alert('⚠️ หากทำการโอนเงินแล้ว กรุณารอสักครู่แล้วลองตรวจสอบอีกครั้ง\n(ระบบอาจใช้เวลา 5-10 นาที)');
                    }
                })
                .catch(error => {
                    loadingOverlay.style.display = 'none';
                    console.error('Error:', error);
                    alert('⚠️ เกิดข้อผิดพลาดในการเชื่อมต่อ กรุณาลองใหม่');
                });
        }

        // Close app using LINE LIFF
        function closeApp() {
            try {
                // Check if LIFF is already initialized from liff-app.js
                window.location.href = '/order/canceled';
            } catch (error) {
                console.error('Error closing window:', error);
                alert('กรุณาปิดหน้าต่างนี้ด้วยตัวเอง');
            }
        }

        // Disable zoom on iOS devices
        if (/iPad|iPhone|iPod/.test(navigator.userAgent)) {
            const viewport = document.querySelector('meta[name="viewport"]');
            viewport.setAttribute('content', viewport.content + ', user-scalable=no');
        }

    </script>
</body>

</html>