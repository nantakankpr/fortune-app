<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ต่ออายุสำเร็จ - Fortune App</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: white;
            min-height: 100vh;
            color: #333;
        }

        .container {
            width: 100%;
            max-width: none;
            margin: 0;
            background: white;
            min-height: 100vh;
            position: relative;
        }

        .header {
            background: linear-gradient(135deg, #48c6ef 0%, #6f86d6 100%);
            padding: 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '🔄';
            position: absolute;
            top: 15px;
            left: 25px;
            font-size: 24px;
            animation: rotate 2s infinite;
        }

        .header::after {
            content: '💎';
            position: absolute;
            top: 15px;
            right: 25px;
            font-size: 24px;
            animation: sparkle 2s infinite 1s;
        }

        @keyframes rotate {
            0%, 100% {
                transform: rotate(0deg);
            }
            50% {
                transform: rotate(180deg);
            }
        }

        @keyframes sparkle {
            0%, 100% {
                transform: scale(1) rotate(0deg);
                opacity: 0.7;
            }
            50% {
                transform: scale(1.3) rotate(180deg);
                opacity: 1;
            }
        }

        .success-title {
            font-size: 32px;
            font-weight: bold;
            color: white;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
            margin-bottom: 8px;
        }

        .success-subtitle {
            font-size: 16px;
            color: rgba(255, 255, 255, 0.9);
        }

        .content {
            padding: 20px 15px;
        }

        .success-icon {
            font-size: 120px;
            margin-bottom: 25px;
            animation: pulse 2s infinite;
            text-align: center;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
        }

        .main-message {
            font-size: 28px;
            font-weight: bold;
            background: linear-gradient(135deg, #48c6ef, #6f86d6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 20px;
            text-align: center;
        }

        .info-card {
            background: linear-gradient(135deg, #e3f2fd 0%, #e1f5fe 100%);
            border-radius: 20px;
            padding: 25px;
            margin: 25px 0;
            color: #2c3e50;
            box-shadow: 0 8px 25px rgba(33, 150, 243, 0.1);
            border: 1px solid rgba(33, 150, 243, 0.1);
        }

        .info-title {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
            color: #34495e;
        }

        .renewal-info {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            border: 1px solid rgba(33, 150, 243, 0.15);
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 15px rgba(33, 150, 243, 0.08);
        }

        .renewal-info:last-child {
            margin-bottom: 0;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
            padding: 8px 0;
            border-bottom: 1px solid rgba(33, 150, 243, 0.1);
        }

        .info-row:last-child {
            margin-bottom: 0;
            border-bottom: none;
        }

        .info-label {
            font-size: 16px;
            font-weight: 600;
            color: #5a6c7d;
        }

        .info-value {
            font-size: 16px;
            font-weight: 700;
            text-align: right;
            color: #2c3e50;
        }

        .package-name {
            font-size: 18px;
            background: linear-gradient(135deg, #2196f3, #1976d2);
            color: white;
            padding: 8px 12px;
            border-radius: 10px;
            display: inline-block;
            font-weight: 700;
            text-shadow: none;
            box-shadow: 0 3px 10px rgba(33, 150, 243, 0.3);
        }

        .price-value {
            font-size: 20px;
            color: #4caf50;
            font-weight: 800;
            text-shadow: none;
        }

        .date-value {
            font-family: monospace;
            font-size: 15px;
            color: #34495e;
        }

        .message-section {
            background: #f0f9ff;
            border-radius: 15px;
            padding: 25px;
            margin: 25px 0;
            border-left: 4px solid #48c6ef;
        }

        .message-title {
            font-size: 20px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .message-content {
            font-size: 16px;
            color: #666;
            line-height: 1.8;
            text-align: left;
        }

        .benefits-section {
            background: white;
            border: 2px solid #e3f2fd;
            border-radius: 15px;
            padding: 25px;
            margin: 25px 0;
            box-shadow: 0 6px 20px rgba(33, 150, 243, 0.1);
        }

        .benefits-title {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .benefits-list {
            list-style: none;
            padding: 0;
        }

        .benefits-list li {
            font-size: 16px;
            color: #666;
            margin-bottom: 10px;
            padding-left: 25px;
            position: relative;
        }

        .benefits-list li:before {
            content: '✅';
            position: absolute;
            left: 0;
            top: 0;
        }

        .renewal-highlight {
            background: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%);
            border-radius: 15px;
            padding: 20px;
            margin: 25px 0;
            text-align: center;
            color: white;
            box-shadow: 0 6px 20px rgba(132, 250, 176, 0.2);
        }

        .highlight-title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .highlight-days {
            font-size: 32px;
            font-weight: bold;
            margin: 10px 0;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .action-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 30px;
        }

        .btn {
            padding: 15px 30px;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            cursor: pointer;
            font-size: 16px;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn-primary {
            background: linear-gradient(135deg, #48c6ef, #6f86d6);
            color: white;
            box-shadow: 0 8px 25px rgba(72, 198, 239, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(72, 198, 239, 0.4);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #a8e6cf, #88d8c0);
            color: #333;
            box-shadow: 0 8px 25px rgba(168, 230, 207, 0.3);
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(168, 230, 207, 0.4);
        }

        .btn-tertiary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }

        .btn-tertiary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(102, 126, 234, 0.4);
        }

        /* Responsive Design */
        @media (min-width: 768px) and (max-width: 1024px) {
            .container {
                max-width: 100%;
                padding: 0;
            }

            .header {
                padding: 40px;
            }

            .success-title {
                font-size: 36px;
            }

            .content {
                padding: 30px 20px;
            }

            .success-icon {
                font-size: 140px;
            }

            .main-message {
                font-size: 32px;
            }
        }

        @media (min-width: 1025px) {
            .container {
                max-width: 600px;
                margin: 0 auto;
                box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
            }

            .header {
                padding: 45px;
            }

            .success-title {
                font-size: 40px;
            }

            .content {
                padding: 35px 25px;
            }

            .success-icon {
                font-size: 150px;
            }

            .main-message {
                font-size: 36px;
            }
        }

        @media (max-width: 767px) {
            .header {
                padding: 25px;
            }

            .success-title {
                font-size: 28px;
            }

            .content {
                padding: 15px 10px;
            }

            .success-icon {
                font-size: 80px;
            }

            .main-message {
                font-size: 24px;
            }

            .btn {
                padding: 12px 25px;
                font-size: 14px;
            }

            .info-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 5px;
            }

            .info-value {
                text-align: left;
            }

            .highlight-days {
                font-size: 28px;
            }
        }

        @media (max-width: 480px) {
            .success-title {
                font-size: 24px;
            }

            .success-icon {
                font-size: 60px;
            }

            .main-message {
                font-size: 20px;
            }

            .highlight-days {
                font-size: 24px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="success-title">ต่ออายุสำเร็จ!</div>
            <div class="success-subtitle">Fortune App - การต่ออายุแพ็คเกจเสร็จสิ้น</div>
        </div>

        <div class="message-section">
            <div class="message-title">
                🎊 ยินดีด้วย! ต่ออายุเรียบร้อย
            </div>
            <div class="message-content">
                การต่ออายุแพ็คเกจของคุณเสร็จสิ้นแล้ว!
                ตอนนี้คุณสามารถใช้บริการ Fortune App ต่อได้อีกครั้ง
                พร้อมรับประสบการณ์ใหม่และคำทำนายที่แม่นยำ
            </div>
        </div>

        <div class="content">
            <div class="success-icon">🎉</div>

            <div class="renewal-highlight">
                <div class="highlight-title">🔄 ต่ออายุสำเร็จแล้ว!</div>
                <div class="highlight-days">ใช้งานได้อีก <%= daysLeft %> วัน</div>
            </div>

            <div class="info-card">
                <div class="info-title">
                    👤 ข้อมูลสมาชิก
                </div>
                <div class="renewal-info">
                    <div class="info-row">
                        <span class="info-label">ชื่อ-นามสกุล:</span>
                        <span class="info-value">
                            <%= userData.full_name %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">เบอร์โทรศัพท์:</span>
                        <span class="info-value">
                            <%= userData.phone || 'ไม่ระบุ' %>
                        </span>
                    </div>
                </div>
            </div>

            <div class="info-card">
                <div class="info-title">
                    📦 ข้อมูลแพ็คเกจที่ต่ออายุ
                </div>
                <div class="renewal-info">
                    <div class="info-row">
                        <span class="info-label">แพ็คเกจ:</span>
                        <span class="info-value package-name">
                            <%= userData.subscriptionData.package_display_name %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">ราคา:</span>
                        <span class="info-value price-value">฿<%=
                                Number(userData.subscriptionData.package_price).toLocaleString() %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">วันที่ต่ออายุ:</span>
                        <span class="info-value date-value">
                            <%= new Date(userData.subscriptionData.start_date).toLocaleDateString('th-TH') %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">วันหมดอายุใหม่:</span>
                        <span class="info-value date-value">
                            <%= new Date(userData.subscriptionData.end_date).toLocaleDateString('th-TH') %>
                        </span>
                    </div>
                </div>
            </div>

            <div class="benefits-section">
                <div class="benefits-title">
                    🎁 สิทธิประโยชน์ที่กลับมาแล้ว
                </div>
                <ul class="benefits-list">
                    <li>รับดวงประจำวันทุกเช้าผ่าน LINE</li>
                    <li>เข้าถึงคำทำนายแบบเจาะลึก</li>
                    <li>รับคำแนะนำชีวิตเฉพาะบุคคล</li>
                    <li>ข้อมูลการใช้งานเดิมยังคงอยู่</li>
                </ul>
            </div>

            <div class="action-buttons">
                <button onclick="closeApp()" class="btn btn-tertiary">
                    🏠 กลับหน้าหลัก
                </button>
            </div>
        </div>
    </div>

    <script>
        // Close app using LINE LIFF
        function closeApp() {
            try {
                if (typeof liff !== 'undefined' && liff.isInClient && liff.isInClient()) {
                    console.log('Closing LIFF window...');
                    liff.closeWindow();
                } else {
                    console.log('LIFF not available, using window.close');
                    window.close();

                    // Fallback
                    setTimeout(() => {
                        window.history.back();
                    }, 500);
                }
            } catch (error) {
                console.error('Error closing window:', error);
                alert('กรุณาปิดหน้าต่างนี้ด้วยตัวเอง');
            }
        }

        // Celebration animation on load
        window.addEventListener('load', function () {
            setTimeout(() => {
                console.log('🎉 ยินดีด้วย! ต่ออายุสำเร็จแล้ว');
            }, 1000);
        });

        // Disable zoom on iOS devices
        if (/iPad|iPhone|iPod/.test(navigator.userAgent)) {
            const viewport = document.querySelector('meta[name="viewport"]');
            viewport.setAttribute('content', viewport.content + ', user-scalable=no');
        }
    </script>

    <!-- LIFF SDK -->
    <% if (typeof config !=='undefined' && config.liffId) { %>
        <script charset="utf-8" src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>
        <script>
            if (typeof liff !== 'undefined') {
                liff.init({
                    liffId: '<%= config.liffId %>'
                }).then(() => {
                    console.log('LIFF initialized for renewal success page');
                }).catch((err) => {
                    console.error('LIFF initialization failed', err);
                });
            }
        </script>
        <% } %>
</body>

</html>