<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สำเร็จ - สมัครแพ็คเกจ | Fortune App</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: white;
            min-height: 100vh;
            color: #333;
        }

        .container {
            width: 100%;
            max-width: none;
            margin: 0;
            background: white;
            min-height: 100vh;
            position: relative;
        }

        .header {
            background: linear-gradient(135deg, #4ecdc4 0%, #44a08d 100%);
            padding: 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '✨';
            position: absolute;
            top: 15px;
            left: 25px;
            font-size: 24px;
            animation: twinkle 2s infinite;
        }

        .header::after {
            content: '🌟';
            position: absolute;
            top: 15px;
            right: 25px;
            font-size: 24px;
            animation: twinkle 2s infinite 1s;
        }

        @keyframes twinkle {

            0%,
            100% {
                transform: scale(1) rotate(0deg);
                opacity: 0.7;
            }

            50% {
                transform: scale(1.3) rotate(180deg);
                opacity: 1;
            }
        }

        .success-title {
            font-size: 32px;
            font-weight: bold;
            color: white;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
            margin-bottom: 8px;
        }

        .success-subtitle {
            font-size: 16px;
            color: rgba(255, 255, 255, 0.9);
        }

        .content {
            padding: 20px 15px;
        }

        .success-icon {
            font-size: 120px;
            margin-bottom: 25px;
            animation: bounce 2s infinite;
            text-align: center;
        }

        @keyframes bounce {

            0%,
            20%,
            53%,
            80%,
            100% {
                transform: translateY(0);
            }

            40%,
            43% {
                transform: translateY(-20px);
            }

            70% {
                transform: translateY(-10px);
            }

            90% {
                transform: translateY(-5px);
            }
        }

        .main-message {
            font-size: 28px;
            font-weight: bold;
            background: linear-gradient(135deg, #4ecdc4, #44a08d);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 20px;
            text-align: center;
        }

        .info-card {
            background: linear-gradient(135deg, #f8f9ff 0%, #e8f4f8 100%);
            border-radius: 20px;
            padding: 25px;
            margin: 25px 0;
            color: #2c3e50;
            box-shadow: 0 8px 25px rgba(52, 152, 219, 0.1);
            border: 1px solid rgba(52, 152, 219, 0.1);
        }

        .info-title {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
            color: #34495e;
        }

        .user-info {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            border: 1px solid rgba(52, 152, 219, 0.15);
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 15px rgba(52, 152, 219, 0.08);
        }

        .package-info {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            border: 1px solid rgba(52, 152, 219, 0.15);
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 15px rgba(52, 152, 219, 0.08);
        }

        .user-info:last-child,
        .package-info:last-child {
            margin-bottom: 0;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
            padding: 8px 0;
            border-bottom: 1px solid rgba(52, 152, 219, 0.1);
        }

        .info-row:last-child {
            margin-bottom: 0;
            border-bottom: none;
        }

        .info-label {
            font-size: 16px;
            font-weight: 600;
            color: #5a6c7d;
        }

        .info-value {
            font-size: 16px;
            font-weight: 700;
            text-align: right;
            color: #2c3e50;
        }

        .package-name {
            font-size: 18px;
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            padding: 8px 12px;
            border-radius: 10px;
            display: inline-block;
            font-weight: 700;
            text-shadow: none;
            box-shadow: 0 3px 10px rgba(52, 152, 219, 0.3);
        }

        .price-value {
            font-size: 20px;
            color: #27ae60;
            font-weight: 800;
            text-shadow: none;
        }

        .date-value {
            font-family: monospace;
            font-size: 15px;
            color: #34495e;
        }

        .message-section {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            margin: 25px 0;
            border-left: 4px solid #4ecdc4;
        }

        .message-title {
            font-size: 20px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .message-content {
            font-size: 16px;
            color: #666;
            line-height: 1.8;
            text-align: left;
        }

        .benefits-section {
            background: white;
            border: 2px solid #e9ecef;
            border-radius: 15px;
            padding: 25px;
            margin: 25px 0;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
        }

        .benefits-title {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .benefits-list {
            list-style: none;
            padding: 0;
        }

        .benefits-list li {
            font-size: 16px;
            color: #666;
            margin-bottom: 10px;
            padding-left: 25px;
            position: relative;
        }

        .benefits-list li:before {
            content: '✅';
            position: absolute;
            left: 0;
            top: 0;
        }

        .contact-info {
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            border-radius: 15px;
            padding: 20px;
            margin: 25px 0;
            box-shadow: 0 6px 20px rgba(252, 182, 159, 0.2);
        }

        .contact-title {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
        }

        .contact-details {
            font-size: 14px;
            color: #666;
        }

        .action-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 30px;
        }

        .btn {
            padding: 15px 30px;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            cursor: pointer;
            font-size: 16px;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn-primary {
            background: linear-gradient(135deg, #4ecdc4, #44a08d);
            color: white;
            box-shadow: 0 8px 25px rgba(78, 205, 196, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(78, 205, 196, 0.4);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(102, 126, 234, 0.4);
        }

        /* Responsive Design */
        @media (min-width: 768px) and (max-width: 1024px) {
            .container {
                max-width: 100%;
                padding: 0;
            }

            .header {
                padding: 40px;
            }

            .success-title {
                font-size: 36px;
            }

            .content {
                padding: 30px 20px;
            }

            .success-icon {
                font-size: 140px;
            }

            .main-message {
                font-size: 32px;
            }
        }

        @media (min-width: 1025px) {
            .container {
                max-width: 600px;
                margin: 0 auto;
                box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
            }

            .header {
                padding: 45px;
            }

            .success-title {
                font-size: 40px;
            }

            .content {
                padding: 35px 25px;
            }

            .success-icon {
                font-size: 150px;
            }

            .main-message {
                font-size: 36px;
            }
        }

        @media (max-width: 767px) {
            .header {
                padding: 25px;
            }

            .success-title {
                font-size: 28px;
            }

            .content {
                padding: 15px 10px;
            }

            .success-icon {
                font-size: 80px;
            }

            .main-message {
                font-size: 24px;
            }

            .btn {
                padding: 12px 25px;
                font-size: 14px;
            }

            .info-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 5px;
            }

            .info-value {
                text-align: left;
            }
        }

        @media (max-width: 480px) {
            .success-title {
                font-size: 24px;
            }

            .success-icon {
                font-size: 60px;
            }

            .main-message {
                font-size: 20px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="success-title">สำเร็จ!</div>
            <div class="success-subtitle">Fortune App - สมัครสมาชิกเรียบร้อย</div>
        </div>

        <div class="message-section">
            <div class="message-title">
                🎊 ขอบคุณที่ไว้วางใจ Fortune App
            </div>
            <div class="message-content">
                ยินดีต้อนรับเข้าสู่ครอบครัว Fortune App!
                ตอนนี้คุณสามารถเข้าถึงฟีเจอร์พิเศษทั้งหมดของเราได้แล้ว
                พร้อมรับประสบการณ์ใหม่ที่จะช่วยให้ชีวิตของคุณดีขึ้น
            </div>
        </div>

        <div class="content">
            <div class="info-card">
                <div class="info-title">
                    👤 ข้อมูลสมาชิก
                </div>
                <div class="user-info">
                    <div class="info-row">
                        <span class="info-label">ชื่อ-นามสกุล:</span>
                        <span class="info-value">
                            <%= userData.full_name %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">เบอร์โทรศัพท์:</span>
                        <span class="info-value">
                            <%= userData.phone || 'ไม่ระบุ' %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">วันที่สมัครสมาชิก:</span>
                        <span class="info-value date-value">
                            <%= new Date(userData.created_at).toLocaleDateString('th-TH') %>
                        </span>
                    </div>
                </div>
            </div>

            <div class="info-card">
                <div class="info-title">
                    📦 ข้อมูลแพ็คเกจ
                </div>
                <div class="package-info">
                    <div class="info-row">
                        <span class="info-label">แพ็คเกจ:</span>
                        <span class="info-value package-name">
                            <%= userData.subscriptionData.package_display_name %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">ราคา:</span>
                        <span class="info-value price-value">฿<%=
                                Number(userData.subscriptionData.package_price).toLocaleString() %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">วันที่สมัคร:</span>
                        <span class="info-value date-value">
                            <%= new Date(userData.subscriptionData.start_date).toLocaleDateString('th-TH') %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">วันหมดอายุ:</span>
                        <span class="info-value date-value">
                            <%= new Date(userData.subscriptionData.end_date).toLocaleDateString('th-TH') %>
                        </span>
                    </div>
                </div>
            </div>

            <div class="contact-info">
                <div class="contact-title">📞 ต้องการความช่วยเหลือ?</div>
                <div class="contact-details">
                    หากคุณมีข้อสงสัยหรือต้องการความช่วยเหลือ<br>
                    สามารถติดต่อทีมงานผ่านไลน์ Official Account ของเราได้ตลอดเวลาทำการ
                </div>
            </div>

            <div class="action-buttons">
                <button onclick="closeApp()" class="btn btn-secondary">
                    🏠 กลับหน้าหลัก
                </button>
            </div>
        </div>
    </div>

    <script>

        // Close app using LINE LIFF
        function closeApp() {
            try {
                if (typeof liff !== 'undefined' && liff.isInClient && liff.isInClient()) {
                    console.log('Closing LIFF window...');
                    liff.closeWindow();
                } else {
                    console.log('LIFF not available, using window.close');
                    window.close();

                    // Fallback
                    setTimeout(() => {
                        window.history.back();
                    }, 500);
                }
            } catch (error) {
                console.error('Error closing window:', error);
                alert('กรุณาปิดหน้าต่างนี้ด้วยตัวเอง');
            }
        }

        // Celebration animation on load
        window.addEventListener('load', function () {
            // Add some celebration effects
            setTimeout(() => {
                console.log('🎉 ยินดีด้วย! สมัครสมาชิกเรียบร้อยแล้ว');
            }, 1000);
        });

        // Disable zoom on iOS devices
        if (/iPad|iPhone|iPod/.test(navigator.userAgent)) {
            const viewport = document.querySelector('meta[name="viewport"]');
            viewport.setAttribute('content', viewport.content + ', user-scalable=no');
        }
    </script>

    <!-- LIFF SDK -->
    <% if (typeof config !=='undefined' && config.liffId) { %>
        <script charset="utf-8" src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>
        <script>
            if (typeof liff !== 'undefined') {
                liff.init({
                    liffId: '<%= config.liffId %>'
                }).then(() => {
                    console.log('LIFF initialized for order success page');
                }).catch((err) => {
                    console.error('LIFF initialization failed', err);
                });
            }
        </script>
        <% } %>
</body>

</html>